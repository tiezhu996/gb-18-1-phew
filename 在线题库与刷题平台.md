## 项目主要需求

### 项目概述

构建一个面向学生的在线题库与刷题平台，支持按学科和知识点进行题目练习，提供顺序练习、随机练习、模拟考试等多种练习模式，自动记录错题并生成个人薄弱知识点分析。平台采用移动端优先设计，适配手机端刷题场景。

### 功能需求

1. **题库管理后台**：管理员可录入和管理题目（单选、多选、判断、填空），题目关联学科分类和知识点标签，支持设置难度等级（简单、中等、困难），支持批量导入题目（Excel/CSV 格式）
2. **学科与知识点体系**：构建多级知识点树结构（学科→章→节→知识点），每个知识点下关联对应题目数量统计，支持搜索知识点快速定位
3. **顺序练习模式**：按知识点顺序依次作答，每题提交后即时显示正确答案和解题解析，支持上一题/下一题切换，自动保存练习进度可中途退出再续
4. **随机练习模式**：按学科或知识点随机抽取指定数量题目进行练习，支持设定题目数量（10/20/50题）和难度范围
5. **模拟考试模式**：模拟真实考试场景——设定考试时间和题目数量，倒计时答题，交卷后一次性显示成绩和答案解析
6. **错题本**：自动收集用户答错的题目，错题按知识点分类归档，支持错题重练（只练习错题），已掌握的错题可手动移除
7. **学习数据分析**：统计用户的练习总量、正确率变化趋势、各知识点掌握程度雷达图，自动识别薄弱知识点并推荐针对性练习

### 技术要求

- 前端：Vue 3 + TypeScript，使用 Vant 4 移动端组件库（适配手机端），Vite 构建工具
- 后端：FastAPI 框架，Python 3.11 开发
- 数据库：MongoDB，使用 Motor 异步驱动
- 缓存：Redis 用于练习进度暂存和排行榜
- 认证方式：JWT 认证
- 图表：前端使用 ECharts 绘制学习数据雷达图和趋势图
- 响应式设计：使用 Vant + 自适应布局适配手机、平板和桌面端

## 代码存放规则

这是前后端分离项目。

前端代码放在 `frontend/`：

页面，组件，路由,状态管理,API 请求,类型定义,样式和资源等放在对应的子文件夹

后端代码放在 `backend/`：

数据库脚本放在 `database/`。

## 部署要求

项目必须支持 Docker Compose 一键部署。

需要提供以下文件：

- 根目录 `docker-compose.yml`：编排前端、后端、数据库及所有依赖服务
- `frontend/Dockerfile`：前端项目构建与运行镜像（多阶段构建，先构建再用 Nginx 托管）
- `backend/Dockerfile`：后端项目构建与运行镜像
- `frontend/nginx.conf`：Nginx 配置，处理前端路由和 API 反向代理
- `.env.example`：所有环境变量的示例文件，附注释说明

docker-compose.yml 要求：

- 所有服务使用命名容器，便于管理
- 数据库数据使用命名卷持久化
- 前端通过 Nginx 反向代理后端 API
- 服务间通过 Docker 内部网络通信
- 数据库服务配置 healthcheck，后端服务依赖数据库健康检查通过后再启动
- 暴露端口（避免与其他项目冲突）：前端 8006，后端 3006，MongoDB 2802，Redis 6406
- 支持通过 `docker compose up -d` 一条命令启动全部服务

## README 写法

根目录必须有 `README.md`，用中文说明项目。

README 包含：

- 项目名称和一句话介绍
- 项目主要功能
- 快速启动方式（Docker Compose 一键部署命令）
- 本地开发方式（前端启动命令、后端启动命令）
- 访问地址
- 技术栈
- 项目目录结构
- 环境变量说明
- Docker 部署说明（端口映射、数据卷、常见问题）
- License

README 要求：

- Docker 部署命令放在最前面，作为首选启动方式
- 本地开发方式作为备选
- 命令可直接复制运行
- 目录结构用代码块展示
- 技术栈用表格展示
- 不要保留脚手架默认 README 内容

